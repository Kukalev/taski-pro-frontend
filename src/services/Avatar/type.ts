/**
 * Тип ответа для пакетной загрузки аватаров.
 * Ключ - username пользователя.
 * Значение - строка Base64 Data URI (например, "data:image/png;base64,...") или null, если аватар не найден.
 */
export type BatchAvatarResponse = Record<string, string | null>;

// --- Больше типов для самого сервиса пока не требуется ---
// Запрос на загрузку использует FormData, ответ пустой (void).
// Получение URL аватара возвращает простую строку (string).
// Если бы мы получали Blob аватара, можно было бы добавить тип для него,
// или если бы у компонента Avatar были специфичные пропсы, их можно было бы описать здесь.

