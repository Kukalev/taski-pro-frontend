export interface ColorOption {
  name: string;
  value: string;
  isSelected: boolean;
}

export interface BackgroundOption {
  id: string;
  name: string;
  previewUrl: string;
  styleValue: string;
} 